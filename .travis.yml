env:
  matrix:
    - "LISP=sbcl"
    - "LISP=ccl"
install:
  - curl -L https://github.com/luismbo/cl-travis/raw/master/install.sh | sh
  - git clone --depth=1 https://github.com/fiddlerwoaroof/daydreamer.git ~/lisp/daydreamer/

script:
  - cl -e '(ql:quickload :daydreamer)'

